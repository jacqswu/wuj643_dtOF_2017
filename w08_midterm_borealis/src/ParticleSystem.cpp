//
//  ParticleSystem.cpp
//  ParticleSystem
//
//  Created by Tyler on 10/5/17.
//
//

#include "ParticleSystem.hpp"

/*
ParticleSystem::ParticleSystem(){
	xSize = 20;
}

void ParticleSystem::update(){ // time elapsed since last frame
	
	// create more particles based on lifespan
//    int nNewParticles = lifespan;
	
	for (int i=0; i<xSize; i++){
		Particle particle();
		particles.push_back(particle);
	}
	
	// update particles
	
}

void ParticleSystem::draw(){
	for (int i=0; i<particles.size(); i++){
		particles[i].draw();
	}
}

for(int x=0; x<xSize; x++){
    
    //Horizontal alpha gradient
    alpha2 = ofMap(x, 0, xSize, 3, 0);

        
        //Illuminating streaks
        ofSetColor(color, alpha2);
        
        ofDrawRectangle(pos.x - width/2 - width*x, pos.y - i, width, 5);
        ofDrawRectangle(pos.x - width/2 + width*x, pos.y - i, width, 5);
        
    }
}
 
 */
